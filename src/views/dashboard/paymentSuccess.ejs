<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <style>
      /* Custom colors based on the dashboard screenshot */
        :root {
          --primary-pink: #e94e77;
          --secondary-pink: #d4429f;
          --teal: #2c5d63;
          --teal-hover: #234a4f;
        }

        /* Gradient header background */
        .gradient-header {
          background: linear-gradient(to right, var(--primary-pink), var(--secondary-pink));
          padding-top: 3rem !important;
          padding-bottom: 3rem !important;
        }

        /* Success icon styling */
        .success-icon-container {
          width: 90px;
          height: 90px;
          position: relative;
          top: 20px;
        }

        .success-icon {
          font-size: 3rem;
          color: var(--primary-pink);
        }

        /* Custom button styles */
        .btn-teal {
          background-color: var(--teal);
          color: white;
          border: none;
        }

        .btn-teal:hover,
        .btn-teal:focus {
          background-color: var(--teal-hover);
          color: white;
        }

        .btn-outline-pink {
          color: var(--primary-pink);
          border-color: var(--primary-pink);
          background-color: transparent;
        }

        .btn-outline-pink:hover,
        .btn-outline-pink:focus {
          background-color: rgba(233, 78, 119, 0.1);
          color: var(--primary-pink);
        }

        /* Animation for success icon */
        .success-icon-container {
          animation: fadeInDown 0.6s ease-out;
        }

        @keyframes fadeInDown {
          from {
            opacity: 0;
            transform: translateY(-20px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }

        /* Card animation */
        .card {
          animation: fadeIn 0.8s ease-out;
        }

        @keyframes fadeIn {
          from {
            opacity: 0;
          }
          to {
            opacity: 1;
          }
        }

        /* Print styles */
        @media print {
          .btn {
            display: none !important;
          }

          .card {
            box-shadow: none !important;
            border: 1px solid #ddd !important;
          }

          .gradient-header {
            background: #f8f9fa !important;
            color: #212529 !important;
          }

          .success-icon {
            color: #212529 !important;
          }
        }


    </style>
</head>
<body class="bg-light">
    <div class="container min-vh-100 d-flex align-items-center justify-content-center py-5">
        <div class="row justify-content-center w-100">
            <div class="col-md-6 col-lg-5 col-xl-4">
                <div class="card border-0 shadow-sm">
                    <!-- Header with gradient background -->
                    <div class="card-header text-center border-0 gradient-header p-4">
                        <div class="success-icon-container bg-white rounded-circle mx-auto d-flex align-items-center justify-content-center">
                            <i class="bi bi-check-circle-fill success-icon"></i>
                        </div>
                    </div>
                    
                    <!-- Card body -->
                    <div class="card-body p-4">
                        <div class="text-center mb-4">
                            <h2 class="fw-bold">Payment Successful!</h2>
                            <p class="text-muted">Your transaction has been processed successfully</p>
                        </div>
                        
                        <!-- Transaction details -->
                        <div class="bg-light rounded p-3 mb-4">
                            <div class="d-flex justify-content-between mb-2">
                                <span class="text-muted">Amount</span>
                                <span class="fw-semibold">₦<%= amount %></span>
                            </div>
                            <div class="d-flex justify-content-between mb-2">
                                <span class="text-muted">Reference</span>
                                <span class="fw-semibold"><%= reference %></span>
                            </div>
                            <div class="d-flex justify-content-between">
                                <span class="text-muted">Date</span>
                                <span class="fw-semibold" id="date">April 4, 2025</span>
                            </div>
                        </div>
                        
                        <!-- Action buttons -->
                        <div class="d-grid gap-2">
                            <a href="/user/dashboard" class="btn btn-teal">Return to Dashboard</a>
                            <button class="btn btn-outline-pink" onclick="window.print()">Download Receipt</button>
                        </div>
                    </div>
                </div>
                
                <p class="text-center text-muted small mt-3">
                    A confirmation email has been sent to your registered email address
                </p>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Simple script to generate reference and date -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Set current date
            const now = new Date();
            const options = { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' };
            document.getElementById('date').textContent = now.toLocaleDateString('en-US', options);
        });
    </script>
</body>
</html>

